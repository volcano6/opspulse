# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Code coverage profiles and other test artifacts
*.out
coverage.*
*.coverprofile
profile.cov

# Dependency directories
# vendor/

# Go workspace file
go.work
go.work.sum

# Build output
bin/
dist/

# Scratch directories used by scripts/verify-onepassword.sh
.opstub-verify/

# Agent workspace (WorkBuddy). Local notes and scratch builds live here; verify
# runs drop throwaway keypairs and built stubs into it, so it must never be
# committed. Keep the directory itself - only version control ignores it.
.workbuddy/

# env file
.env
.env.*

# Editor/IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Frontend (v0.5)
frontend/node_modules/
frontend/dist/
